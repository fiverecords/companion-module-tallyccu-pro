// Feedbacks for TallyCCU Pro
// This file exists for template compatibility

module.exports = async function (self) {
	self.setFeedbackDefinitions({
		// No feedbacks defined - all state is handled via variables
	})
}
